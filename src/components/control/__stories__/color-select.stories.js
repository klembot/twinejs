import ColorSelect from '../color-select';

export default {title: 'Control/<color-select>'};

export const basic = () => ({
	components: {ColorSelect},
	template: '<color-select>Label</color-select>'
});
