const i18n = {
	addResourceBundle: jest.fn(() => i18n),
	changeLanguage: jest.fn(() => i18n),
	createInstance: jest.fn(() => i18n),
	init: jest.fn(() => i18n),
	use: jest.fn(() => i18n)
};

export default i18n;
